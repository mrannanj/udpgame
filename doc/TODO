REFACTOR:
- add Weffc++ warning
- deglobalize menus and screens
- AABB class
- protobuf integration
  - 3 UGLY input classes
  - 2 world classes
  - investigate if possible to use only protobuf classes for components
- refactor object deletion for speed
- grid position class
- refactor so that input handler ticks nothing
- refactor input continous actions

BUGS:
- invisible walls
- wall climb

FEATURES:
- monsters that follow player
- implement handle system for faster component lookups
- maybe add team?
- make possible to set one block per click
- show wielded item in gui
- show in gui if client is in observer or player mode
- add screen that shows key bindings
- server timed
- named players
- fullscreen
- bigger world
  - fixed point math
  - transfer inputs only
- metaserver
- lol chat
- screenshot taking
